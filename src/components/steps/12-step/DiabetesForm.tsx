// Step 12

import React from 'react';
import { CheckerNextStep } from '../../../shared/interfaces/form';

export const DiabetesForm = ({ disableNextStep }: CheckerNextStep) => {
  return (
    <div>
      <h2>Tem diabetes ?</h2>
    </div>
  );
};
