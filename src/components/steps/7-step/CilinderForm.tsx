// Step 7

import React from 'react';
import { CheckerNextStep } from '../../../shared/interfaces/form';

export const CilinderForm = ({ disableNextStep }: CheckerNextStep) => {
  return (
    <div>
      <h2>Apresenta cilindros ?</h2>
    </div>
  );
};
