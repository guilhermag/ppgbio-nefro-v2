// Step 7

import React from 'react';
import { CheckerNextStep } from '../../../shared/interfaces/form';

const CilinderForm = ({ disableNextStep }: CheckerNextStep) => {
  return (
    <div>
      <h2>Apresenta cilindros ?</h2>
    </div>
  );
};

export default CilinderForm;
